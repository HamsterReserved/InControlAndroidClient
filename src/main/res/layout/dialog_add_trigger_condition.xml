<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="30dp"
    android:paddingLeft="24dp"
    android:paddingRight="24dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/when"
        android:textSize="16sp"
        android:textColor="@color/primary_text_default_material_light"
        android:id="@+id/tv_when"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <Spinner
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/spinner_when"
        android:layout_alignBaseline="@+id/tv_when"
        android:layout_toRightOf="@+id/tv_when"
        android:layout_toEndOf="@+id/tv_when"
        android:entries="@array/when" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/et_when"
        android:inputType="numberDecimal|numberSigned"
        android:layout_alignBaseline="@id/tv_when"
        android:layout_toEndOf="@id/spinner_when"
        android:layout_toRightOf="@id/spinner_when" />

</RelativeLayout>
